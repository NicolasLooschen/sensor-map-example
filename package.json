{
  "name": "sensor-map",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": ["sensor-map-client", "sensor-map-server"],
  "scripts": {
    "dev:client": "npm run dev -w sensor-map-client",
    "dev:server": "npm run dev -w sensor-map-server",
    "dev": "npm run dev:client & npm run dev:server",
    "build": "npm run build -w sensor-map-client && npm run build -w sensor-map-server",
    "lint": "biome check .",
    "format": "biome format --write .",
    "check": "biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4"
  }
}
